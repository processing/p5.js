DOM Extensions
==========

###DOM manipulation & access
+ var AUTO;

####Create 
+ createHTML(html)
+ createHTMLImage(src)
+ createGraphics(w, h)

####Access
+ find(id) // return arr
+ find(class) // return arr

####Set context
+ context(id)
+ context(obj)


####PElement

+ id(id)
+ class(class)
+ size(w, h) 
+ position(x, y)
+ style(style)
+ html(html)
+ show()
+ hide()

// Input
+ mousePressed(function)
+ mouseOver(function)
+ mouseOut(function)



