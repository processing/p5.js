/**
 * @module ? 
 * @submodule ? 
 * @requires ? 
 */
define(function (require) {

  'use strict';

  var p5 = require('core');
  
  p5.Shader = function () {
   this.vertexSource
   this.fragmentSource
  };
}
